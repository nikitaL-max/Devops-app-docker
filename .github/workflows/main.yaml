# # 1. The name of the workflow displayed on the GitHub Actions dashboard tab
# name: Build and Push Docker Image

# # 2. TRIGGER: Defines when this automated script should run
# on:
#   push:
#     branches: [ "main" ]  # Automatically triggers whenever code is pushed to the 'main' branch

# # 3. JOBS: The set of tasks to execute
# jobs:
#   build-and-push:
#     # RUNNER: Spawns a fresh, temporary Ubuntu Linux cloud machine in GitHub's datacenter
#     runs-on: ubuntu-latest

#     # STEPS: The sequential instructions the runner executes
#     steps:
    
#     # STEP 1: Downloads a copy of your source code (Dockerfile, index.html, etc.) into the runner
#     - name: Checkout Repository
#       uses: actions/checkout@v4

#     # STEP 2: Authenticates with Docker Hub using your encrypted GitHub Secrets
#     - name: Log in to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKER_USERNAME }}  # Pulls your Docker Hub username from secrets
#         password: ${{ secrets.DOCKER_PASSWORD }}  # Pulls your Docker Hub password or Personal Access Token

#     # STEP 3: Compiles your Dockerfile into an Image and uploads it to Docker Hub
#     - name: Build and Push Docker Image
#       uses: docker/build-push-action@v5
#       with:
#         context: .                                               # Looks in the current folder for the Dockerfile
#         push: true                                              # Set to 'true' to upload to Docker Hub (if 'false', it only builds)
#         tags: ${{ secrets.DOCKER_USERNAME }}/my-nginx-site:latest # Formats the image name on Docker Hub